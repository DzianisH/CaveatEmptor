spring:
  jpa:
    hibernate:
      ddl-auto: update
      naming:
        implicit-strategy: org.hibernate.boot.model.naming.ImplicitNamingStrategyComponentPathImpl
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/caveatemptor?useUnicode=true&serverTimezone=UTC&useSSL=true&verifyServerCertificate=false